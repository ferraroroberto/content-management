"""Illustration copyright check — reverse-image search, store, and triage queue.

See ``check_ip/README.md``. The store is SQLite (``check_ip.db``), the search
pipeline is ``check_ip.run``, the screening queue the skill drives is
``check_ip.screen``, and the control-panel surface is ``app/tab_check_ip.py``.
"""
